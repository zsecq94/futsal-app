import SafeAreaWrapper from "@/components/shared/safe-area-wrapper";
import { Text } from "@/utils/theme";
import React from "react";

const MatchScreen = () => {
  return (
    <SafeAreaWrapper>
      <Text>매치 스크린 입니다.</Text>
    </SafeAreaWrapper>
  );
};

export default MatchScreen;
